# Production config (see _config_dev.yml for local dev config file)

# Site settings
title: Flutter - Dashboard
email: yjbanov@google.com
description: Flutter project dashboard.
baseurl: "/flutter-dashboard/current" # the subpath of your site, e.g. /blog
url: "https://storage.cloud.google.com" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
